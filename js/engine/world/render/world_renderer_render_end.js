"use strict";

function WorldRendererEnd(worldrenderer) {
    this.worldrenderer = worldrenderer;
}

WorldRendererEnd.prototype.renderEnd = function(now, mbr, window, graphics, camera, world, debug) {
}